# Epic 10: 手机 App

> Phase: 5 (P1)

面向普通人的产品，手机才是主场。用户不会天天开电脑看 Dashboard，但手机随时在手边——收到通知看一眼、扫个码连 VPN、出门前确认服务还活着。Passim 手机 App 不是 Web UI 的缩小版，而是围绕"随手管理"这个场景重新设计的。

---

## US-10.1 连接 Passim 节点 `P1` `Phase 5`

**作为** 用户
**我想** 在手机上添加我的 Passim 节点
**以便** 随时查看和管理

**验收标准:**
- [ ] 首次打开 App，引导页说明 Passim 是什么 + 如何开始 (1-2 屏)
- [ ] 添加节点方式:
  - 手动输入: 地址 (host:port) + API Key
  - 扫码添加: Web UI 设置页生成包含地址 + API Key 的二维码，App 扫码一步完成
- [ ] 添加后自动连接验证，成功后进入 Dashboard
- [ ] 支持添加多个节点，底部 Tab 或侧滑切换
- [ ] 节点列表显示: 名称、地址、在线/离线状态
- [ ] 离线节点标灰，定期自动重试连接

**前端交互:**
```
┌─────────────────────────────┐
│                             │
│         Passim              │
│   你的个人云管理助手        │
│                             │
│  ┌───────────────────────┐  │
│  │  📷 扫码连接          │  │
│  └───────────────────────┘  │
│                             │
│  ┌───────────────────────┐  │
│  │  ✏️ 手动输入          │  │
│  └───────────────────────┘  │
│                             │
│  在 VPS 上运行:             │
│  docker run ... passim      │
│  然后用 Web UI 生成连接码   │
│                             │
└─────────────────────────────┘
```

---

## US-10.2 Dashboard 概览 `P1` `Phase 5`

**作为** 用户
**我想** 在手机上一眼看到所有节点的状态
**以便** 随时掌握我的云服务是否正常

**验收标准:**
- [ ] 当前节点状态卡片: CPU / 内存 / 磁盘 (环形图或进度条)
- [ ] 网络流量: 上行/下行实时速率
- [ ] 已部署应用列表: 名称 + 运行状态指示灯
- [ ] 多节点时，顶部横向滑动切换节点 (类似银行卡切换)
- [ ] 下拉刷新
- [ ] 数据通过 Passim API 获取 (`GET /api/status`)

**前端交互:**
```
┌─────────────────────────────┐
│  tokyo-1          🟢 Online │
├─────────────────────────────┤
│                             │
│   CPU     MEM      DISK    │
│  ┌───┐  ┌───┐   ┌───┐     │
│  │45%│  │62%│   │40%│     │
│  └───┘  └───┘   └───┘     │
│                             │
│  ↓ 5.2 MB/s   ↑ 1.8 MB/s  │
│                             │
├─────────────────────────────┤
│  应用                       │
│                             │
│  🛡 WireGuard        🟢    │
│  🔒 L2TP             🟢    │
│  📁 WebDAV            🔴    │
│                             │
├─────────────────────────────┤
│  ⌂ Home  📱 Apps  ⚙ Settings│
└─────────────────────────────┘
```

---

## US-10.3 应用管理与快捷操作 `P1` `Phase 5`

**作为** 用户
**我想** 在手机上启停应用、部署新应用
**以便** 不用开电脑也能管理服务

**验收标准:**
- [ ] 应用列表页: 所有已部署应用，点击进入详情
- [ ] 应用详情页:
  - 运行状态 + 资源占用 (CPU / 内存)
  - 启动 / 停止 / 重启 按钮 (确认弹窗)
  - 客户端配置入口 (→ US-10.4)
- [ ] 部署新应用:
  - 模板网格 (复用 Web 的模板数据 `GET /api/templates`)
  - 参数表单 (根据模板动态生成，适配手机屏幕)
  - 部署进度页 (SSE 实时更新)
- [ ] 多节点时可选择部署到哪个节点

---

## US-10.4 VPN 配置与扫码 `P0` `Phase 5`

**作为** 用户
**我想** 在手机上直接获取 VPN 配置并导入客户端
**以便** 不用在电脑和手机之间来回传文件

这是手机 App 最核心的场景——大部分用户装 Passim 就是为了用 VPN，而 VPN 配置的最终消费者就是手机。

**验收标准:**
- [ ] WireGuard 配置:
  - 显示配置文件列表 (peer1.conf, peer2.conf...)
  - 点击 → 弹出选项: "导入到 WireGuard" / "显示二维码" / "分享"
  - "导入到 WireGuard": 通过 deep link 或系统分享直接打开 WireGuard App
  - 二维码: 全屏显示，自动调高亮度
- [ ] L2TP 配置:
  - 显示连接参数 (服务器 / 用户名 / 密码 / PSK)
  - 一键复制各字段
  - iOS: "安装描述文件" 按钮 (下载 .mobileconfig)
- [ ] Hysteria 配置:
  - "导入到 Stash/Shadowrocket" (通过 URL scheme)
  - 显示配置内容 + 复制
- [ ] 通用:
  - 配置页能保存到本地，离线也能查看
  - 选择哪个节点的配置 (多节点场景)

**前端交互:**
```
WireGuard Configs — tokyo-1

┌─────────────────────────────┐
│                             │
│  📄 peer1.conf              │
│  ┌───────────────────────┐  │
│  │ 导入到 WireGuard App  │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ 📷 显示二维码          │  │
│  └───────────────────────┘  │
│  ┌───────────────────────┐  │
│  │ ↗ 分享                │  │
│  └───────────────────────┘  │
│                             │
│  📄 peer2.conf              │
│  ...                        │
│                             │
└─────────────────────────────┘

--- 二维码全屏 ---
┌─────────────────────────────┐
│                             │
│         peer1.conf          │
│                             │
│      ┌───────────────┐      │
│      │               │      │
│      │   QR Code     │      │
│      │               │      │
│      └───────────────┘      │
│                             │
│  用 WireGuard App 扫描      │
│                             │
│        [ 关闭 ]             │
│                             │
└─────────────────────────────┘
```

---

## US-10.5 推送通知 `P2` `Phase 5`

**作为** 用户
**我想** 在手机上收到重要事件通知
**以便** 及时处理问题，不用一直盯着 Dashboard

**验收标准:**
- [ ] 通知类型:
  - 节点离线 / 恢复上线
  - 容器异常停止
  - SSL 证书即将过期 (7 天前)
  - 部署完成 / 失败
  - 有新版本 Passim 可用
- [ ] 通知设置:
  - 可按类型开关通知
  - 可设置勿扰时段
  - 可按节点单独配置
- [ ] 点击通知跳转到对应页面
- [ ] 实现方式: Expo Push Notifications + Passim API 注册设备 token

**后端支持 (Passim 侧):**
```
POST /api/push/register    { "token": "ExponentPushToken[xxx]", "device": "iPhone 16" }
DELETE /api/push/register  { "token": "..." }
GET /api/push/settings     → 通知偏好
PUT /api/push/settings     → 更新通知偏好
```

---

## US-10.6 Passkey 生物认证 `P1` `Phase 5`

**作为** 用户
**我想** 用指纹或面容登录 App
**以便** 安全又方便，不用每次输 API Key

**验收标准:**
- [ ] 首次添加节点用 API Key 认证 (→ US-10.1)
- [ ] 认证成功后提示注册 Passkey: "使用 Face ID / 指纹登录？"
- [ ] 注册后，下次打开 App 直接生物认证，不显示 API Key 输入
- [ ] 使用 WebAuthn API (与 Web 端共用 Passim 的 Passkey 后端)
- [ ] App 锁: 切到后台超过 5 分钟，返回时需要重新认证
- [ ] 设置页可管理 Passkey (查看/删除/重新注册)
